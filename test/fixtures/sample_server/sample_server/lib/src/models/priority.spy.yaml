### Task priority. Default `byIndex` serialization (wire form is the int index).
enum: Priority
values:
  ### Lowest priority.
  - low
  ### Default priority.
  - normal
  ### Highest priority.
  - high
