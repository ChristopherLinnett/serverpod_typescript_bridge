### Task priority. `byIndex` serialization (wire form is the int index).
enum: Priority
serialized: byIndex
values:
  ### Lowest priority.
  - low
  ### Default priority.
  - normal
  ### Highest priority.
  - high
