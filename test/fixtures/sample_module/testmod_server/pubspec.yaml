name: testmod_server
description: Starting point for a Serverpod module.

environment:
  sdk: '^3.8.0'

resolution: workspace

dependencies:
  serverpod: 3.4.7

dev_dependencies:
  lints: '>=3.0.0 <7.0.0'
  serverpod_test: 3.4.7
  test: ^1.25.5
