/// serverpod_typescript_bridge
///
/// Generates a TypeScript client package for a Serverpod project, mirroring
/// the behaviour of `serverpod generate` for the Dart client.
///
/// The public API surface is intentionally minimal at this stage — the package
/// is consumed via its CLI (`dart run serverpod_typescript_bridge`).
library;
